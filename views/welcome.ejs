<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chartjs-plugin-datalabels"></script>
    <link rel="stylesheet" href="/styles.css">
    <title>NEXUS ID</title>
    <style>
        .container {
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: rgba(149, 182, 119, 0.9);
        }
        .createCard {
            width: 60%;
            padding-left: 24px;
            border-radius: 18px;
            box-shadow: none;
        }
        .ctrl a.pbtn:hover {
            background-color: transparent;
            color: green;
            cursor: pointer;
        }
        .formCard.left {
            padding-right: 20px;
            position: relative;
        }
        .displayCard {
            width: 70%;
            height: 80%;
            background: linear-gradient(45deg, #28a745, #167c8c);
            box-shadow: 0 8px 6px rgba(0, 0, 0, 0.4);
            border: 0.5px solid rgba(255, 255, 255, 0.451);
            position: absolute;
            top: 10%;
            left: 15%;
            transform: rotate(-10deg);
            border-radius: 14px;
            z-index: 3;
        }
        .displayIdCard {
            width: 70%;
            height: 80%;
            border: 0.5px solid rgba(255, 255, 255, 0.451);
            box-shadow: 0 4px 30px rgba(255, 255, 255, 0.4);
            backdrop-filter: blur(8px);
            background: rgba(255, 255, 255, 0.2);
            border: 1px solid white;
            position: absolute;
            top: 10%;
            left: 15%;
            border-radius: 14px;
            z-index: 4;
            transform: rotate(5deg);
            display: flex;
            justify-content: flex-start;
            align-items: center;
            padding: 25px;
            color: white;
            font-size: 14px;
            flex-direction: column;
        }
        .idPhoto {
            position: absolute;
            display: flex;
            align-items: center;
            justify-content: center;
            width: 25%;
            height: 20%;
            background-color: transparent;
            border-radius: 20px;
            top: 25%;
            right: 37.5%;
            box-shadow: 0 6px 8px rgba(0, 0, 0, 0.1);
            z-index: 4;
            border: 0.5px solid white;
            transform: rotate(5deg);
        }
        .idPhoto img {
            height: 90%;
            width: 90%;
            opacity: 0.5;
            border-radius: 500px;
            border: 1px solid white;
        }
        #submit:hover {
            transform: scale(1.03);
        }
    </style>
</head>
<body>
    <div class="container">
        <form class="createCard" id="createCard" action="/login" method="POST">
            <div class="formCard left">
               <div class="displayCard"></div>
               <div class="displayIdCard">
                    Local Government Unit<br>
                    Science City of Munoz
                    <span style="margin-top: 150px; font-size: 18px; text-align: center;">EMPLOYEE ID <br> Management System</span>
               </div>
               <div class="idPhoto">
                   <img src="/images/logo.png">
               </div>
            </div>
    
            <div class="formCard right">
                <div class="row">
                    <div class="field" style="justify-content: center; align-items: center;">
                        <fh>Log in Your Account</fh>
                    </div>
                </div>
                <!-- Display error message if there is one -->
                <% if (typeof error !== 'undefined' && error) { %>
                    <div style="color: orangered; font-size: 12px; text-align: center;">
                        <%= error %>
                    </div>
                <% } %>
                <br>
                <div class="row">
                    <div class="field" style="position: relative">
                        <p3>Username</p3>
                        <input type="text" name="username" required>
                    </div>
                </div>
                <div class="row">
                    <div class="field" style="position: relative">
                        <p3>Password</p3>
                        <input type="password" id="new-password" name="password" required>
                        <span class="toggle-password" onclick="togglePassword('new-password')">show</span>
                    </div>
                </div>

                <div class="row ctrl" style="justify-content: center;">
                    <span class="plainBtn"><a href="/forgotForm" class="pbtn" style="border: none; padding: 0; margin: 0;">Forgot Password?</a></span>
                </div>
                <div class="row ctrl" style="justify-content: center;">
                    <button style="width: 100%; max-width: 100%; font-size: 16px; border-radius: 8px; cursor: pointer;" type="submit" id="submit">
                        LOGIN
                    </button>
                </div>
                <div class="row ctrl" style="justify-content: center;">
                    Don't have an account? <span class="plainBtn"><a href="/register" class="pbtn" style="border: none; padding: 0; margin: 0;">Register now!</a></span>
                </div>
            </div>
        </form>
    </div>

    <script>
        function togglePassword(inputId) {
            const passwordInput = document.getElementById(inputId);
            const toggleText = passwordInput.nextElementSibling;

            if (passwordInput.type === "password") {
                passwordInput.type = "text";
                toggleText.textContent = "hide";
            } else {
                passwordInput.type = "password";
                toggleText.textContent = "show";
            }
        }
    </script>
</body>
</html>
